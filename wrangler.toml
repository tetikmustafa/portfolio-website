# Cloudflare Pages configuration for @cloudflare/next-on-pages
# Build command: npx @cloudflare/next-on-pages
# nodejs_compat is OFF — all edge routes use pure Web APIs (fetch, ReadableStream, etc.)

name = "portfolio-website"
compatibility_date = "2024-09-23"

[vars]
# Environment variables are set in Cloudflare Pages dashboard, not here.
# Required vars: GEMINI_API_KEY, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, TURNSTILE_SECRET_KEY
